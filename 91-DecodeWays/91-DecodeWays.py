                for i in range(1, 27):
                    
                    if len(cur) == 1 and s[0] == cur:
                        res += dp(s[1:])
            else:
                res = 0
                    cur = str(i)
                memo[s] = 1
                pass
            elif s == "":
        def dp(s):
            if s in memo:
class Solution:
    def numDecodings(self, s: str) -> int:
        memo = {}

"

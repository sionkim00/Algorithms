        for src, neighbors in adj.items():
            if len(neighbors) == 1:
        leaves = deque()
        edge_cnt = {}

            adj[b].append(a)
                leaves.append(src)
            edge_cnt[src] = len(neighbors)
4

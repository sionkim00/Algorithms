            elif i == 0:
                for k in range(N):
            elif j < 0 or j >= N:
                memo[key] = float("inf")
                memo[key] = float("inf")
            elif i == N:
                memo[key] = 0
            if key in memo:
                pass
            key = (i,j)
        memo = defaultdict()
        def dp(i, j):
class Solution:
    def minFallingPathSum(self, matrix: List[List[int]]) -> int:
        N = len(matrix)
[

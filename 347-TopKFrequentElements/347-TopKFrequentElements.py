        for key in counter:
            heapq.heappush(heap, [-1*counter[key], 
        ans = []
            cur = heapq.heappop(heap)
        for i in range(k):
            ans.append(cur[1])
        heap = []
        heapq.heapify(heap)
key])
        return ans
[

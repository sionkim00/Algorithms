
        for a, b in trust:
            kvp[b].add(a)

        for key in kvp:
            if not(b in kvp):
                kvp[b] = set()
        trustsSomeone = defaultdict(bool)
            trustsSomeone[a] = True
    def findJudge(self, n: int, trust: List[List[int]]) -> int:
        kvp = defaultdict()
class Solution:

        if n == 1:
            return 1
2

                cur_max = 0
                res = 0
                for j in range(i, min(len(arr), i+k)):
            else:
                memo[i] = 0
            elif i >= N:
                pass
        def dp(i):
            if i in memo:
        memo = defaultdict()

class Solution:
    def maxSumAfterPartitioning(self, arr: List[int], k: int) -> int:
        N = len(arr)
[

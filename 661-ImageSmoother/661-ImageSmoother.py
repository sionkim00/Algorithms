
        res = [[0] * M for _ in range(N)]
        M = len(img[0])
        N = len(img)
1],[1,-1],[1,0],[1,1]]
        neighboringCells = [[-1,-1],[-1,0],[-1,1],[0,-1],[0,0],[0,
[int]]:
    def imageSmoother(self, img: List[List[int]]) -> List[List
class Solution:
[
